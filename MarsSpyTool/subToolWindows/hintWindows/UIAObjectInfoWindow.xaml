<Window x:Class="MarsSpyTool.subToolWindows.hintWindows.UIAObjectInfoWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MarsSpyTool.subToolWindows.hintWindows"
        mc:Ignorable="d"
        Title="UIA Object Info" 
        Height="400" 
        Width="600" 
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        x:Name="uiaInfoWindow">
    <Border Background="#E0FFFFFF" 
            BorderBrush="#FF808080" 
            BorderThickness="1" 
            CornerRadius="5"
            Opacity="0.8"
            x:Name="mainBorder">
        <Grid Margin="10">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <TextBlock Grid.Row="0" 
                       Text="UIA Object Properties" 
                       FontWeight="Bold" 
                       FontSize="14" 
                       Margin="5"/>
            <ScrollViewer Grid.Row="1" 
                          VerticalScrollBarVisibility="Auto" 
                          HorizontalScrollBarVisibility="Auto">
                <TextBlock x:Name="objectInfoTextBlock" 
                           TextWrapping="Wrap" 
                           FontFamily="Consolas" 
                           FontSize="11"
                           Margin="5"/>
            </ScrollViewer>
        </Grid>
    </Border>
</Window>

