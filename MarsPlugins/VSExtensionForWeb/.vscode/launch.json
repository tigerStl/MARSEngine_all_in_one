{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run Extension (MARS Web Automation Engine)",
      "type": "extensionHost",
      "request": "launch",
      "args": [
        "--extensionDevelopmentPath=${workspaceFolder}/vscode-extension-web"
      ]
    }
  ]
}

