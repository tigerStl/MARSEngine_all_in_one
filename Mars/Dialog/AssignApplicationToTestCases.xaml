<Window x:Class="Mars.Dialog.AssignApplicationToTestCases"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Mars.Dialog"
        mc:Ignorable="d"
        ResizeMode="NoResize"
        Title="MARS Assign Application To Test Cases" Height="140" Width="400" WindowStyle="ToolWindow"
        WindowStartupLocation="CenterScreen">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"></RowDefinition>
            <RowDefinition Height="auto"></RowDefinition>
            <RowDefinition Height="auto"></RowDefinition>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="120"></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <Label Content="Select Applications:" Grid.Row="0" Grid.Column="0" Margin="10" HorizontalAlignment="Left"></Label>
        <ComboBox ItemsSource="{Binding AllMarsApplications}"
                  DisplayMemberPath="APP_SHORT_NAME"
                  SelectedItem="{Binding SelectedMarsApplictions}"
                  Grid.Row="0"
                  Grid.Column="1"
                  Margin="10"
                  
                  ></ComboBox>
        <Label Content="Project Name:" Grid.Row="1" Grid.Column="0" Margin="5" HorizontalAlignment="Left"></Label>
        <Label Content="{Binding ProjectName}" Grid.Row="1" Grid.Column="1" Margin="5" HorizontalAlignment="Left"></Label>

        <StackPanel FlowDirection="LeftToRight" Grid.Column="0" Grid.Row="2" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="8">
            <Button Content="Ok" Width="100" Margin="5" Command="{Binding AssingAppToTCCommand}"></Button>
            <Button Content="Cancel" Width="100" Margin="5" Command="{Binding CloseCommand}"></Button>
        </StackPanel>
        
    </Grid>
</Window>
