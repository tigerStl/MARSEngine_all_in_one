<Window x:Class="Mars.Dialog.FillEditWizardDialog1"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="430" d:DesignWidth="500" Height="430" Width="500" Title="Table Name: Chashflows">


    <Window.Resources>
        <Style x:Key="GridSplitterPreviewStyle" >
            <Setter Property="Control.Template">
                <Setter.Value>
                    <ControlTemplate>
                        <Rectangle Fill="#80000000"/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="GridSplitterStyle1" TargetType="{x:Type GridSplitter}">
            <Setter Property="Background"
                Value="{DynamicResource {x:Static SystemColors.ControlBrushKey}}"/>
            <Setter Property="PreviewStyle" Value="{StaticResource GridSplitterPreviewStyle}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type GridSplitter}">
                        <Border BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            Background="{TemplateBinding Background}"
                            CornerRadius="5">
                            <Canvas RenderOptions.EdgeMode="Aliased" UseLayoutRounding="True"
                                Height="6" VerticalAlignment="Center"
                                Width="50" HorizontalAlignment="Center">
                                <Line X1="0" X2="50" Y1="0" Y2="0"
                                  Stroke="White" StrokeThickness="1"/>
                                <Line X1="0" X2="50" Y1="1" Y2="1"
                                  Stroke="#A0A0A0" StrokeThickness="1"/>
                                <Line X1="0" X2="50" Y1="4" Y2="4"
                                  Stroke="White" StrokeThickness="1"/>
                                <Line X1="0" X2="50" Y1="5" Y2="5"
                                  Stroke="#A0A0A0" StrokeThickness="1"/>
                            </Canvas>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="GridSplitterVer" TargetType="{x:Type GridSplitter}">
            <Setter Property="Background"
                Value="{DynamicResource {x:Static SystemColors.ControlBrushKey}}"/>
            <Setter Property="PreviewStyle" Value="{StaticResource GridSplitterPreviewStyle}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type GridSplitter}">
                        <Border BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            Background="{TemplateBinding Background}"
                            CornerRadius="5" >
                            <Canvas RenderOptions.EdgeMode="Aliased" UseLayoutRounding="True"
                                Height="16" VerticalAlignment="Center"
                                Width="7" HorizontalAlignment="Center">
                                <Line X1="0" X2="0" Y1="0" Y2="50"
                                  Stroke="White" StrokeThickness="1"/>
                                <Line X1="1" X2="1" Y1="0" Y2="50"
                                  Stroke="#A0A0A0" StrokeThickness="1"/>
                                <Line X1="4" X2="4" Y1="0" Y2="50"
                                  Stroke="White" StrokeThickness="1"/>
                                <Line X1="5" X2="5" Y1="0" Y2="50"
                                  Stroke="#A0A0A0" StrokeThickness="1"/>
                            </Canvas>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SpinButton"  TargetType="Slider">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Slider">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="20"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="*"/>
                                <RowDefinition Height="1"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>
                            <TextBox x:Name="txtRow"  Grid.RowSpan="3" Text="{Binding Value,Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}" VerticalContentAlignment="Center" BorderThickness="1,1,0,1" TextChanged="TextBox_TextChanged" HorizontalAlignment="Left" Width="30"/>
                            <RepeatButton Grid.Column="1" Grid.Row="0" Command="Slider.IncreaseLarge">
                                <Path Fill="Black" Data="M0,1 L0.5,0 1,1z" Width="9" Height="5" Stretch="Fill" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                            </RepeatButton>
                            <Rectangle Grid.Column="1" Grid.Row="1" Fill="{TemplateBinding BorderBrush}"/>
                            <RepeatButton Grid.Column="1" Grid.Row="2" Command="Slider.DecreaseLarge">
                                <Path Fill="Black" Data="M0,0 L0.5,1 1,0z" Width="9" Height="5" Stretch="Fill" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                            </RepeatButton>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <RowDefinition Height="30"/>
            <RowDefinition Height="30"/>
            <RowDefinition Height="30"/>
            <RowDefinition/>
            <RowDefinition Height="50"/>
            <RowDefinition Height="50"/>
        </Grid.RowDefinitions>


        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition Width="10"/>
            <ColumnDefinition Width="120"/>
            <ColumnDefinition Width="150"/>
        </Grid.ColumnDefinitions>

        <!-- Row 0 -->
        <DockPanel x:Name="descriptionOpenPanel" Grid.Row="0" Grid.ColumnSpan="3" Height="40" VerticalAlignment="Top" >
            <Label Content="Description: Specify table cell location." Height="30" Width="300" Margin="10" FontWeight="Bold" FontSize="14"></Label>
        </DockPanel>

        <!-- Row 1 -->
        <Label Grid.Row="1" Grid.Column="0" Margin="10,0,0,0">Keyword:</Label>
        <Label Grid.Row="1" Grid.Column="2" Margin="10,0,0,0" Content="{Binding SelectedKeyword}" FontWeight="Bold" />
        
        <!-- Row 2 -->
        <Label Grid.Row="2" Grid.Column="0" Margin="10,0,0,0">Object:</Label>
        <Label Grid.Row="2" Grid.Column="2" Margin="10,0,0,0" Content="{Binding SelectedObject}" FontWeight="Bold"  />

        <!-- Row 3 -->
        <Label Grid.Row="3" Grid.Column="0" Margin="10,0,0,0">Column Name:</Label>
        
        <!-- Row 4 -->
        <DockPanel x:Name="treePanel" Grid.Row="4" Grid.Column="0" >
            <TreeView   HorizontalAlignment="Left"
	                    Height="145"
	                    Margin="10,10,0,0"
	                    VerticalAlignment="Top"
	                    Width="180"
	                    Loaded="TreeView_Loaded"
	                    SelectedItemChanged="TreeView_SelectedItemChanged"
	                   />
        </DockPanel>

        <GridSplitter Grid.Row="4" 
                          Grid.Column="1" 
                          ResizeDirection="Columns"  
                          HorizontalAlignment="Stretch" 
                          Style="{DynamicResource GridSplitterVer}" Margin="2,0"
                          />

        <StackPanel x:Name="panel1" Grid.Row="4"  Grid.Column="2"  Margin="5" VerticalAlignment="Stretch" >
            <RadioButton x:Name="rbRowNumber" Margin="10" Click="rbRowNumber_Click">Row Number</RadioButton>
            <RadioButton x:Name="rbLastRow" Margin="10" Click="rbLastRow_Click">Last Row</RadioButton>
            <Label Content="Control Type:" Height="30" Width="70" Margin="10,5,5,5" HorizontalAlignment="Left"></Label>
        </StackPanel>

        <StackPanel x:Name="panel2" Grid.Row="4"  Grid.Column="3"   Margin="5" VerticalAlignment="Stretch" >
            <Slider x:Name="spinButton" Height="25" Width="50" Margin="10" Value="{Binding RowValue}" Style="{StaticResource SpinButton}" HorizontalAlignment="Left"/>
            <ComboBox Name="lstType"  Margin="10,35,0,10" Width="100" HorizontalAlignment="Left"
                  ItemsSource="{Binding TypeList}" SelectedItem="{Binding SelectedType}"  />
        </StackPanel>

        <!-- Row 5 -->
        <DockPanel x:Name="resultPanel" Grid.Row="5" Grid.ColumnSpan="3" Height="40" VerticalAlignment="Top" >
            <Label Content="Result:" Height="30" Width="70" Margin="10"></Label>
            <TextBox x:Name="txtResilt" HorizontalAlignment="Left" Width="200"  Text="{Binding RowColValue}" Height="25" Margin="10,0"></TextBox>
        </DockPanel>

        <!-- Row 6 -->
        <DockPanel x:Name="buttonPanel" Grid.Row="6" Grid.Column="5" Grid.ColumnSpan="3" Height="40" VerticalAlignment="Top" >
            <Button IsDefault="True" Name="btnDialogOk"  Width="70"  MinWidth="60" Height="25" Margin="0,0,10,0" Click="btnDialogOk_Click">Ok</Button>
            <Button IsCancel="True" Height="25"  Width="70" MinWidth="60">Cancel</Button>
        </DockPanel>
    </Grid>
</Window>
