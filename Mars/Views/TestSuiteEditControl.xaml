<mars:MarsBaseViewControl x:Class="Mars.Views.TestSuiteEditControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:mars="clr-namespace:Mars.Views.baseView"
             xmlns:local="clr-namespace:Mars.ViewModel"
             xmlns:con="clr-namespace:Mars.Converter"
             mc:Ignorable="d" 
            >
    <UserControl.Resources>
        <con:WidthConverter x:Key="widthConverter"></con:WidthConverter>
    </UserControl.Resources>
    <!--<Border BorderBrush="LightGray" BorderThickness="1" CornerRadius="5">-->
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="*" MinHeight="200"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>
        <!--Defined Column-->
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"></ColumnDefinition>
            <ColumnDefinition Width="Auto"></ColumnDefinition>
            <ColumnDefinition Width="*"></ColumnDefinition>
            <ColumnDefinition Width="Auto"></ColumnDefinition>
        </Grid.ColumnDefinitions>

        <DockPanel Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="3">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition></RowDefinition>
                    <RowDefinition></RowDefinition>
                    
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="auto"></ColumnDefinition>
                    <ColumnDefinition></ColumnDefinition>
                </Grid.ColumnDefinitions>
                <Label Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2"  Content="Test Suite Edit" FontSize="20" Margin="5" x:Name="lblHeader" ></Label>
                
            </Grid>
        </DockPanel>
        <!--First Column Content-->
        <Label Content="Test Case" Grid.Column="0" Grid.Row="1" Margin="5" ></Label>
        <TextBox Grid.Column="2" Grid.Row="1" Margin="0,5,10,5"  Text="{Binding FilterName, UpdateSourceTrigger=PropertyChanged}" 
                ></TextBox>
        <!--Second Column Content-->
        <ListView Name="listTestCase" Grid.Column="2" Grid.Row="2" Margin="0,5,10,5"  MaxHeight="600"
                  ScrollViewer.HorizontalScrollBarVisibility="Disabled" 
                  ItemsSource="{Binding Path=TestCase}"                  
                  ScrollViewer.VerticalScrollBarVisibility="Auto" >
            <ListView.Resources>
                <Style TargetType="{x:Type GridViewColumnHeader}">
                    <Setter Property="HorizontalContentAlignment" Value="Left" />
                </Style>
            </ListView.Resources>
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Select" Width="50">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <CheckBox IsChecked="{Binding IsSelected}"/>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                    <GridViewColumn Header="Test Case" Width="200" DisplayMemberBinding="{Binding TEST_CASE_NAME}" />
                </GridView>
            </ListView.View>
        </ListView>

        <Label Grid.Row="0" Grid.Column="3" Content="Applications:"  Margin="5,5,15,5" HorizontalAlignment="Left" VerticalAlignment="Bottom"></Label>
        <TextBlock Grid.Row="1" Grid.Column="3" Grid.RowSpan="2" VerticalAlignment="Top"
                           Text="{Binding ApplicationInfo}"
                           Margin="5,5,15,5"></TextBlock>
        <WrapPanel Grid.Column="2" Grid.ColumnSpan="2" Grid.Row="8" >
            <Button Content="Save" Width="70" Height="24" Margin="5" Command="{Binding SaveCommand}"></Button>
            <Button Content="Cancel" Width="70" Height="24" Margin="5" Command="{Binding ClearCommand}"></Button>
        </WrapPanel>

        <!--Third Column Content-->

    </Grid>
    <!--</Border>-->
</mars:MarsBaseViewControl>
