<mars:MarsBaseViewControl x:Class="Mars.Views.user.management.UserAndCompanyManagement"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"             
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Mars.Views.user.management"
             xmlns:mars="clr-namespace:Mars.Views.baseView"
             xmlns:converters="clr-namespace:Mars.Converter"
             mc:Ignorable="d"                           
             d:DesignHeight="550" d:DesignWidth="700">
    <UserControl.Resources>
        
        <converters:MarsPwdConvert x:Key="pwdConvert"/>
        
        <Style TargetType="TextBox">
            <Setter Property="BorderBrush" Value="LightGray"></Setter>
            <Setter Property="MinWidth" Value="160"></Setter>
        </Style>
        <Style TargetType="PasswordBox">
            <Setter Property="BorderBrush" Value="LightGray"></Setter>
            <Setter Property="MinWidth" Value="160"></Setter>
        </Style>
        <Style TargetType="{x:Type DataGridColumnHeader}">
            <Setter Property="VerticalContentAlignment" Value="Center" />
            <Setter Property="Height" Value="30" />
            <Setter Property="SeparatorBrush" Value="LightGray" />            
            <Setter Property="FontWeight" Value="DemiBold" />
            <Setter Property="HorizontalContentAlignment" Value="Center"></Setter>
        </Style>
        
    </UserControl.Resources>

    <Border Margin="5,0,5,0" BorderBrush="LightGray" BorderThickness="0.7" CornerRadius="5">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"></RowDefinition>
                <RowDefinition></RowDefinition>
            </Grid.RowDefinitions>
            <!--top, list of all all user-->
            <Grid Grid.Row="0">
                <Grid.RowDefinitions>
                    <RowDefinition></RowDefinition>
                    <RowDefinition Height="auto"></RowDefinition>
                </Grid.RowDefinitions>
                <Border Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="4" BorderThickness="0,0,0,0.7" BorderBrush="LightGray" Margin="0,0,5,0">
                    <Label Content="All MARS User List:" Margin="5" FontWeight="Bold" FontSize="16" x:Name="lblTitle"></Label>
                </Border>
                <DataGrid Grid.Row="1" Height="200" Margin="5" BorderThickness="1" BorderBrush="LightGray" GridLinesVisibility="All" HorizontalGridLinesBrush="LightGray" VerticalGridLinesBrush="LightGray"
                          ItemsSource="{Binding UsersList}" AutoGenerateColumns="False" CanUserAddRows="False" FrozenColumnCount="1" 
                          IsReadOnly="True" EnableColumnVirtualization="True" 
                          SelectedItem="{Binding CurrentUser}"
                          >
                    
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="User Name" Binding="{Binding TESTER_LOGIN_NAME}"  MinWidth="64"></DataGridTextColumn>
                        <DataGridTextColumn Header="Company" MinWidth="100" Binding="{Binding COMPANY_NAME}"></DataGridTextColumn>

                        <DataGridTextColumn Header="First Name" Binding="{Binding TESTER_NAME_F}"></DataGridTextColumn>
                        <DataGridTextColumn Header="Middle Name" Binding="{Binding TESTER_NAME_M}"></DataGridTextColumn>
                        <DataGridTextColumn Header="Last Name" Binding="{Binding TESTER_NAME_L}"></DataGridTextColumn>
                        <DataGridTextColumn Header="Email" MinWidth="100" Binding="{Binding TESTER_MAIL}"></DataGridTextColumn>
                        <DataGridTextColumn Header="Phone" Binding="{Binding TESTER_NUMBER}"></DataGridTextColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>

            <Grid Grid.Row="1">
                <Grid.RowDefinitions>
                    <RowDefinition Height="auto">

                    </RowDefinition>
                    <RowDefinition Height="auto"></RowDefinition>
                    <RowDefinition Height="auto"></RowDefinition>
                    <RowDefinition Height="auto" Name="xx"></RowDefinition>
                    <RowDefinition Height="auto"></RowDefinition>
                    <RowDefinition></RowDefinition>
                    <RowDefinition ></RowDefinition>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="105"></ColumnDefinition>
                    <ColumnDefinition Width="1*"></ColumnDefinition>
                    <ColumnDefinition Width="105"></ColumnDefinition>
                    <ColumnDefinition Width="1*"></ColumnDefinition>
                </Grid.ColumnDefinitions>
                <Border Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="4" BorderThickness="0,0,0,0.7" BorderBrush="LightGray" Margin="0,0,5,0">
                    <DockPanel>
                        <Label Content="Create/Modify MARS User Information:  Company Name:" Margin="5" FontWeight="Bold" FontSize="16"></Label>
                        
                    </DockPanel>
                </Border>
                <Label Grid.Row="1" Grid.Column="0" Content="Login Name:" Margin="5"></Label>
                <Label Grid.Row="2" Grid.Column="0" Content="First Name:" Margin="5"></Label>
                <Label Grid.Row="3" Grid.Column="0" Content="Middle Name:" Margin="5"></Label>
                <Label Grid.Row="4" Grid.Column="0" Content="Last Name:" Margin="5"></Label>
                <Label Grid.Row="5" Grid.Column="0" Content="Email:" Margin="5"></Label>
                
                <Label Grid.Row="1" Grid.Column="2" Content="Password:" Margin="5"></Label>
                <Label Grid.Row="2" Grid.Column="2" Content="Re-type Password:" Margin="5"></Label>
                <Label Grid.Row="3" Grid.Column="2" Content="Phone Number:" Margin="5"></Label>
                <Label Grid.Row="4" Grid.Column="2" Content="Description:" Margin="5"></Label>

                <DockPanel Grid.Row="1" Grid.Column="1" >
                    <TextBox Margin="5" Text="{Binding CurrentUser.TESTER_LOGIN_NAME}" DockPanel.Dock="Left" IsEnabled="{Binding CanUserNameBeEdited}"></TextBox>
                </DockPanel>
                <TextBox Grid.Row="2" Grid.Column="1" Margin="5" Text="{Binding CurrentUser.TESTER_NAME_F}"></TextBox>
                <TextBox Grid.Row="3" Grid.Column="1" Margin="5" Text="{Binding CurrentUser.TESTER_NAME_M}"></TextBox>
                <TextBox Grid.Row="4" Grid.Column="1" Margin="5" Text="{Binding CurrentUser.TESTER_NAME_LAST}" Height="{Binding Path=Height, ElementName=mPhoneNumber}" VerticalAlignment="Top"></TextBox>
                <TextBox Grid.Row="5" Grid.Column="1" Margin="5" Text="{Binding CurrentUser.TESTER_MAIL}" Height="{Binding Path=Height, ElementName=mPhoneNumber}" VerticalAlignment="Top"></TextBox>
                
                <PasswordBox Grid.Row="1" Grid.Column="3" Margin="5" Name="PwdEdt"></PasswordBox>
                <PasswordBox Grid.Row="2" Grid.Column="3" Margin="5" Name="PwdEdt_rept"></PasswordBox>
                <TextBox Grid.Row="3" Grid.Column="3" Margin="5" Text="{Binding CurrentUser.TESTER_NUMBER}" Name="mPhoneNumber"></TextBox>
                <TextBox Grid.Row="4" Grid.Column="3" Margin="5" Text="{Binding CurrentUser.TESTER_DESC}" 
                         Grid.RowSpan="2" VerticalAlignment="Top"
                         AcceptsReturn="True" Height="90" TextWrapping="WrapWithOverflow"></TextBox>
                <Border Grid.Row="6" Grid.Column="0" Grid.ColumnSpan="4" BorderThickness="0,0.7,0,0" BorderBrush="LightGray" Margin="5,0,5,0">
                    <DockPanel LastChildFill="False" Height="40" VerticalAlignment="Top">
                        <Button Content="Save" DockPanel.Dock="Right" Width="80" Margin="5" Command="{Binding SaveUserInformation}">
                            <Button.CommandParameter>
                                <MultiBinding Converter="{StaticResource pwdConvert}">
                                    <Binding ElementName="PwdEdt"></Binding>
                                    <Binding ElementName="PwdEdt_rept"></Binding>
                                </MultiBinding>
                            </Button.CommandParameter>
                        </Button>
                        <Button Content="Delete" DockPanel.Dock="Right" Width="80" Margin="5" Command="{Binding DelUserInformation}" >
                        </Button>
                        <Button Content="Clear" DockPanel.Dock="Right" Width="80" Margin="5" Command="{Binding ClearUserInformation}" >
                        </Button>
                    </DockPanel>
                </Border>
            </Grid>
        </Grid>
    </Border>
</mars:MarsBaseViewControl>
