<mars:MarsBaseViewControl x:Class="Mars.Views.DataSheetLink"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Mars.ViewModel"
             xmlns:con="clr-namespace:Mars.Converter"
             xmlns:mars="clr-namespace:Mars.Views.baseView"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300" Width="500" HorizontalAlignment="Left">
    <!--Title="Link Dataset to Test Case"-->
    <UserControl.Resources>
        <con:WidthConverter x:Key="widthConverter"></con:WidthConverter>
    </UserControl.Resources>
    <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"></RowDefinition>
                <RowDefinition Height="Auto"></RowDefinition>
                <RowDefinition Height="Auto"></RowDefinition>
            </Grid.RowDefinitions>
            <!--Defined Column-->
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"></ColumnDefinition>
                <ColumnDefinition Width="Auto"></ColumnDefinition>
                <ColumnDefinition Width="Auto"></ColumnDefinition>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="3" Margin="10">
                <Label Content="Link Dataset to Test Case" FontSize="20" x:Name="lblTitle"></Label>
            </StackPanel>
            <!--First Column Content-->

            <!--Second Column Content-->
            <ListView Name="listDataSheet" Grid.Column="2" Grid.Row="1" Margin="20,5,-57,10" 
                  ScrollViewer.HorizontalScrollBarVisibility="Disabled" 
                  ItemsSource="{Binding Path=LinkedDataSheet}"                  
                  ScrollViewer.VerticalScrollBarVisibility="Auto" MaxHeight="100">
                <ListView.Resources>
                    <Style TargetType="{x:Type GridViewColumnHeader}">
                        <Setter Property="HorizontalContentAlignment" Value="Left" />
                    </Style>
                </ListView.Resources>
                <ListView.View>
                    <GridView>
                        <GridViewColumn Header="Select" Width="50">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <CheckBox IsChecked="{Binding IsSelected}"/>
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                        <!-- Make sure DATA_SHEET_NAME is available in model !!-->
                        <GridViewColumn Header="Dataset" Width="200" DisplayMemberBinding="{Binding DataItemName}" />
                    </GridView>
                </ListView.View>
            </ListView>


            <WrapPanel Grid.Column="2" Grid.Row="3  " Margin="15">
                <Button Content="Save" Width="70" Height="24" Margin="5" Command="{Binding SaveCommand}"/>
                <Button Content="Cancel" Width="70" Height="24" Margin="5" Command="{Binding ClearCommand}"/>
            </WrapPanel>

            <!--Third Column Content-->

        </Grid>
    </ScrollViewer>
</mars:MarsBaseViewControl>
