<mars:MarsBaseViewControl x:Class="Mars.Views.TestResultTabView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Mars.Views"
             xmlns:mars="clr-namespace:Mars.Views.baseView"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300" PreviewKeyDown="UserControl_PreviewKeyDown"
             >
    
    <Grid >

        <Grid.RowDefinitions>
            <RowDefinition></RowDefinition>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="32" x:Name="contolPanelColumn"/>
        </Grid.ColumnDefinitions>
        
        <TabControl x:Name="tabcntrlResults" ItemsSource="{Binding ResultTestReport}" Margin="0,2,0,2" TabStripPlacement="Bottom" >
            <TabControl.ItemContainerStyle>
                <Style TargetType="TabItem">
                    <Setter Property="ContentTemplate" >
                        <Setter.Value>
                            <DataTemplate>
                                <DockPanel>
                                    <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">
                                        <local:TestResultsViewControl></local:TestResultsViewControl>
                                    </ScrollViewer>
                                </DockPanel>
                            </DataTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </TabControl.ItemContainerStyle>
            <TabControl.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding Header}"></TextBlock>
                </DataTemplate>
            </TabControl.ItemTemplate>         
        </TabControl>
        <StackPanel x:Name="controlPanel" Grid.Row="1" Grid.Column="1" Margin="2,0,2,0" Orientation="Vertical" >
            <Button FontFamily="Times New Roman" FontWeight="Black" FontSize="20" Click="SaveButton_Click" >
                <StackPanel>
                    <Image Source="/Images/excel-32.png"  ToolTip="Save Test Report to Excel file (Ctrl+S)"  />
                </StackPanel>
            </Button>
        </StackPanel>
    </Grid>
</mars:MarsBaseViewControl>
