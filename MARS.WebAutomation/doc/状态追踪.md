# MARS.WebAutomation 状态追踪（WBS）

最后更新：2026-04-26

## WBS 与完成状态

| ID | 任务 | 状态 | 备注 |
|----|------|------|------|
| W1 | 建立 .NET Framework 4.7.2 类库工程与 NuGet（Playwright、Newtonsoft.Json） | 已完成 | 使用 SDK 风格 `net472` 类库，确保 PackageReference 正确参与编译 |
| W2 | 编写 doc/需求.md、doc/详细设计.md、README.md | 已完成 | |
| W3 | 实现数据模型与 `data\[domain]_key\test_[url].json` 存储 | 已完成 | `DataPathHelper` + `JsonDataStore` |
| W4 | 实现 PlaywrightHost + 导航 + URL 解析 Tab | 已完成 | Target page Tab |
| W5 | 实现对象树采集与 Object Tab（Tree + Grid） | 已完成 | `PageInspectionScripts` + 左右分割 |
| W6 | 实现录制（Binding + InitScript）与语义映射 + Record Tab | 已完成 | `RecordingService` + DataGridView |
| W7 | 实现回放引擎与 Toolbar 联动 | 已完成 | `ReplayService` |
| W8 | 实现 XHR/Fetch 捕获并写入 JSON | 已完成 | `NetworkCaptureService` |
| W9 | 实现 Settings Tab 与可配置数据根目录 | 已完成 | `%AppData%\MARS.WebAutomation\settings.json` |
| W10 | 主窗体：Menu、Toolbar 分区、Export/Import/Save | 已完成 | `MainWorkbenchForm` |
| W11 | Record 页：左右分割 + 步骤表列模型（删除/序号/耗时/事件/布局/逻辑对象） | 已完成 | 2026-04-26 |
| W12 | Record 页：WebBrowser 流程画布 + 关键字配色 + 节点拖动 + `CanvasX/Y` 落盘 | 已完成 | 2026-04-26 |
| W13 | 录制脚本：`blur` 提交文本、抑制无意义 click/input、完整 CSS Locator、`LogicalKind` | 已完成 | `recorder.install.js` + `RecordingService` |
| W14 | 步骤行序：右键上移/下移、重编号、与画布同步 | 已完成 | 2026-04-26 |
| W15 | 文档：`需求.md` / `详细设计.md` / `功能列表.md` / `状态追踪.md` 增量追加 Record 增强 | 已完成 | 2026-04-26 |
| W16 | 录制：`SelectTab` 语义、Tab 向上层数配置、文本 Data 补全；对象属性末行 `outerHTML` | 已完成 | 2026-04-26 |

## 变更日志

- 2026-04-23：创建 WBS 与初始文档骨架。
- 2026-04-23：补齐主窗体与对外入口；工程改为 SDK 风格 `net472` 以修复旧式 csproj 下 NuGet 引用未参与编译的问题；`dotnet build` 通过。
- 2026-04-26：Record/Replay 工作台增强（分割布局、流程画布、语义录制与步骤表列）；见 `doc/需求.md` §8 与 `doc/详细设计.md` §10。
- 2026-04-26：`SelectTab` 录制语义、设置项 `RecorderTabContextAncestorDepth`、`SemanticStepRecord` 的 Target* 字段、对象表末行 `outerHTML`；见 `doc/详细设计.md` §10.2–10.6。
